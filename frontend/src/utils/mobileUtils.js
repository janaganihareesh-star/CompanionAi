// Capacitor has been removed. We are migrating to React Native (Expo) for mobile.
// These functions are kept as empty stubs so that the web frontend doesn't break.

export const isNativeMobile = () => false;

export const triggerHapticLight = async () => {};

export const triggerHapticMedium = async () => {};

export const hideKeyboard = async () => {};

export const setStatusBarStyle = async (isDarkMode) => {};
